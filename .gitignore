# Anything that smells like a key — defense in depth, this repo should
# never contain credentials in the first place.
*.pem
*.pem.age
*.key
config
.env
.env.*

# Allow the (non-secret) test fixture key.
!tests/fixtures/fake-key.pem

# Test sandbox dirs (in case any leak)
tests/tmp/
